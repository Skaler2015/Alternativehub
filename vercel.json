{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "prisma generate && prisma migrate deploy && tsx scripts/seed-if-empty.ts && tsx scripts/backfill-details.ts && tsx scripts/expand-catalog.ts && next build",
  "installCommand": "npm ci",
  "crons": [
    { "path": "/api/cron/maintenance", "schedule": "0 2 * * *" }
  ]
}
